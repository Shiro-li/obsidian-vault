#git #github 
  
查看目前狀態：  
git status  
  
加入所有修改：  
git add .  
  
建立新版本：  
git commit -m "更新內容"  
  
上傳更新到 GitHub：  
git push  
  
刪除檔案後同步：  
git add .  
git commit -m "刪除檔案"  
git push  
  
下載最新版本：  
git pull  
  
理解：  
add = 放進待提交清單  
commit = 建立版本紀錄  
push = 上傳到 GitHub  
pull = 從 GitHub 拉回最新版本